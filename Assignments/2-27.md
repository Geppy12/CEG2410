Planning:
## Ports:

What ports need open inbound and from what sources?
A: 80/443 -/0 (HTTP/80 -/0 HTTPS) for web services, 22 (SSH) -shell acces to the server -Wright State/Home IP
for Linux management

What ports need open outbound and to what destinations?
- ephermal(temporary or short-lived.) ports (returning traffic to clients) 
- 1025 - 65535 - TCP (Transmission Control Protocol)
- /0 all traffic allowd out on client servers
- Question: Do I need to update the server (HHTPS / 443)

What level(s) will you be handling firewall access and what needs to be configured?
- NACL (Network Access Control List): 
- SG (securtiy groups):
- System:


## Configuration:
What files will you need to edit to configure your web serving service?
- appachie2.cof ()
- Ngnix (ngnix.cof) service level configuration
How will you configure the web serving service?
 I will install the role via Server Manager set up a local content folder define site bindings (port, IP), and configure SSL for security.

Are there additional modifications on the server - /etc/hosts, DNS, etc. - that you need to make to support the virtual hosts?
No, their is not any other support you need to add to a vm. Just configure the DNS.

How do you control the deamon process for the web server? How do you start, stop, and restart the service?
Use sudo 1. systemctl start <service>, 2. sudo systemctl stop <service>, and 3. sudo systemctl restart <service> to manage the service.


## Location and access:
Where does the web serving service look for web content based on the configuration files?
A: The web server looks for web content in a specific local file system path that is defined in its configuration files, as the web root or document root. This location is found by a configuration directive, which varies depending on the web server software used.

You have TWO sites to host, how will you organize the content for each site on the server?
A: To host two sites on one server, organize content using virtual hosting (Apache/Nginx) to map separate domains to distinct root directories
(domains meaning website URL)

What user and group will the web server process run as, and how does that affect file permissions?
A: Web server processes typically run as a low-privileged user to enhance security. 

What permissions will you need to set on the content directories and files to allow the web server to serve them?
A: For web servers (Apache/Nginx on Linux), set directories to 755 (rwxr-xr-x) and files to 644 (rw-r--r--) to allow the server to read/execute files while preventing unauthorized modification.

How are you going to allow developers to update the content on the server?
A: Developers can update server content using secure, direct, or automated methods SSH for command-line access, and CMS platforms for content management. 
FTP and SFTP if good for file transfer 

Will you use FTP, SFTP, or some other method?
A: I would use the SFTP (secure transfer file protocol) its in the name secure. I would use this

What permissions will the developers need to have on the server to update the content?
A: I would give them permissions to rewrtie and modify the content. The developers built the site. 